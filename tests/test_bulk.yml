cache:
    connection_params:
        host: localhost
    collection: backache
    db: backache
resource:
    strict:
          host: localhost
          port: 6379
          db: 0
    uri: backache://{operation}/{uri}

celery:
    CELERY_ALWAYS_EAGER: true
