mongo:
    connection_params:
        host: localhost
    collection: backache
    db: backache
redis:
    strict:
          host: localhost
          port: 6379
          db: 0
    uri: backache://{operation}/{uri}
